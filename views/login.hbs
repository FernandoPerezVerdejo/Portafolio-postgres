
{{#> layout}}
<body class="cuerpo-pagina-login">
    <main class="proyector-login">
        <form class="formulariologin" action="/app" method="post">
            <div class="form-floating mb-3 has-validation">
                <input type="text" class="form-control is-invalid"
                    name="username"
                    id="floatingInput1 username"
                    placeholder="name" required>
                <label for="floatingInput">Usuario</label>
                <div class="invalid-feedback">
                    Por favor ingresar Usuario.
                </div>
            </div>
            <div class="form-floating has-validation">
                <input type="password" class="form-control is-invalid"
                    name="password"
                    id="floatingPassword1 password" placeholder="Password"
                    required>
                <label for="floatingPassword">Contraseña</label>
                <div class="invalid-feedback">
                    Por favor ingresar Contraseña.
                </div>
            </div>
            <ul class="botonlogin">
                <button type="submit" class="btn btn-primary">Iniciar
                    Sesión</button>
            </ul>
            <ul class="registro"><a class="link" href="register?"
                    style="color:blue">Registrarse</a></ul>
        </form>
        <div>
            {{#if exitoso}}
            <h1>Login Exitoso</h1>
            {{/if}}
            {{#if error}}
            <h1>Usuario o Contraseña Incorrectos</h1>
            {{/if}}
        </div>
    </main>
</body>
{{/layout}}
